{#
/**
 * @file
 * HP Graph paragraph.
 *
 * @ingroup themeable
 */
#}

<section class="section--padded-top section--padded-bottom"  aria-label="Graph">
    {% if hp_show_wrapper %}
        <section class="compartment">
    {% endif %}
			<section class="hp_graph_bundle">
				<div class="text col">
					<aside class="text_wrapper">
						<p class="lead-in">{{ content.field_hp_graph_title[0] }}</h1>
						<p>{{ content.field_hp_graph_description[0] }}</p>
						{%  if true in content.field_hp_graph_link[0] %}
							<a class="btn" href="{% spaceless %}{{-content.field_hp_graph_link[0]['#url']-}}{% endspaceless %}">{{ content.field_hp_graph_link[0]['#title'] }}</a>
						{% endif %}
					</aside>
				</div>
				<div class="graph col">
					{% for i in 0..content.field_hp_graph_data|length %}
						{% if content.field_hp_graph_data[i] is not null %}
						{{ content.field_hp_graph_data[i] }}
						{% endif %}
					{% endfor %}
				</div>
			</section>
	{% if hp_show_wrapper %}
        </section>
    {% endif %}
</section>
